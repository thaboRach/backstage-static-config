# Backstage Static Configuration

An example repository that would have all the static configuration files for Backstage

https://backstage.io/docs/features/software-catalog/configuration/#static-location-configuration
